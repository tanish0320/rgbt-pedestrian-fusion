# Install torch/torchvision/mmcv-full FIRST per README.md (CUDA-version-specific,
# not listed here) — then:  pip install -r requirements.txt
mmdet==2.25.1
numpy<2
opencv-python<4.10
scipy
gdown
pycocotools
matplotlib
terminaltables
# yapf>=0.40 dropped FormatCode's `verify` kwarg, which mmcv==1.6.1's Config.pretty_text
# still passes — breaks tools/train.py (and anything calling cfg.dump()) with
# "TypeError: FormatCode() got an unexpected keyword argument 'verify'". Pin below avoids it.
yapf==0.32.0
